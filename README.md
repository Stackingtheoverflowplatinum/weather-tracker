The project takes data from the Open-Meteo API and turns the data into CSV files 
I built it so I would be able to get climate data about Grand Canyon to be used later
It uses data from the Open-Meteo API
